import React from 'react';

const TestRoute = () => (
  <div>
    <h1>Test route updated text</h1>
    <p> see if tag gets skipped</p>
    <p> lets see if I can merge</p>
  </div>
);

export default TestRoute;
